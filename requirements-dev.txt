# Dev + test tooling (not needed at runtime).
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.4.1
ruff==0.16.0
mypy==2.3.0
psycopg[binary]==3.3.4                 # applying SQL migrations (scripts/apply_migration.py)
